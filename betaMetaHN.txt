model{
      for (j in 1:Nstud) {
      Y[j] ~ dnorm(delta[j],P[j])
      delta[j] ~ dnorm(d,prec)
      }
      P[1] ~ dgamma(1.88,1)
      d ~ dnorm(0.0,prec.d)
      OR<-exp(d)
      tau~dnorm(0,prec.tau)I(0,)
      tau.sq<-tau*tau
      prec<-1/(tau.sq)
      d.new ~dnorm(d,prec)
}